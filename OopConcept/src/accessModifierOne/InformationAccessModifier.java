package accessModifierOne;

public class InformationAccessModifier {
 /* default modifier:No keyword having in default modifier, behavior will not allow to you one package to another package, wont allow to use method
  * public modifier :Access anywhere in class or other package
  * private modifier:Within class only, private to particular class only.
  * protected modifier:Allow to access one package to another package, we need to use inheritance.

  */
}
